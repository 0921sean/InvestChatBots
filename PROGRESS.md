# PROGRESS — InvestChatBots (살아있는 진행상황)

> **매 세션 프로토콜:** 시작 시 이 파일을 먼저 읽고, 끝에 갱신한다.
> (CLAUDE.md = *고정 지침·가드레일* / 이 파일 = *매 세션 바뀌는 진행상황·다음 할 일*)
> `claude -p` 는 스테이트리스라, 이 파일이 세션 간 기억이다.
> ⚠️ **ICB는 라이브 사이트다.** 코드 변경 전 Think Before Coding + Surgical Changes 준수(CLAUDE.md §A).
> updated: 2026-09-02 (#156·#157 머지 · 8/28 인스타 유입 버스트 · 정기 체크포인트 규칙 · 🔒 보안 감사 1회차 · Git 컨벤션 개정)
>
> **이 파일 구조 (읽는 순서):**
> 1. **🧭 시스템 전체 구성** — 지금 시스템이 어떻게 생겼나. **현재 사실의 단일 진실원천.**
> 2. **❓ 미답 제안** — 사용자 답을 기다리는 판단들. 휘발 금지.
> 3. **📚 이하 히스토리** — 어떻게 여기까지 왔나. 옛 서술이 섞여 있으니 1번과 충돌하면 1번이 이긴다.
>
> 구조를 바꿨으면 **1번부터** 고칠 것.
>
> ⚠️ **갱신을 세션 끝까지 미루지 않는다.** `/clear`·컨텍스트 압축은 예고 없이 온다.
> PR 머지 / 미답 제안 발생 / 구조 변경 / **코드에 안 남는 발견**(운영 관측·버그 원인·사용자 행동) /
> 사용자가 정리를 언급 — 하나라도 나오면 **그 자리에서** 갱신·커밋.
> 상세는 [CLAUDE.md](CLAUDE.md) 상단 "📌 정기 체크포인트".

---

# 🧭 시스템 전체 구성 (2026-08-24 검증 기준)

> **이 섹션은 "지금 시스템이 어떻게 생겼나"의 단일 지도다.** 아래 세션 로그는 *어떻게 여기까지 왔나*(히스토리)라서 옛 내용이 섞여 있다 — **현재 사실은 이 섹션이 우선**이다.
> 값은 전부 코드·DB에서 확인한 것. 구조가 바뀌면 이 섹션부터 고칠 것.
> ⚠️ **전략가 실명 금지**(public 레포). 봇은 역할어 + 공개 레터로만 쓴다.

## 1. 한 줄 요약
투자 스타일이 다른 AI 봇들이 **가상 포트폴리오**로 모의 매매하고, 그 과정을 사람이 관전하는 라이브 서비스. 모든 신규 매수는 **오너 결재**를 거친다. 실거래 아님.

## 2. 배포 위상
```
investchatbots.com ──cloudflared──► Mac mini : uvicorn main:app :8001 (launchd 관리)
                                      ├─ FastAPI (단일 프로세스)
                                      ├─ APScheduler (인프로세스, KST)
                                      └─ SQLite 단일 파일 (39 테이블)
LLM = `claude` CLI 서브프로세스 (구독 토큰 버킷 — 크레딧 과금 아님. ANTHROPIC_API_KEY 제거하고 호출)
```
- **배포 = 워킹카피**. 현재 라이브는 브랜치 `feat/admin-console` 워킹카피로 구동 중.
- 재시작은 `launchctl kickstart -k com.investchatbots.app` 또는 `pkill -f "uvicorn main:app"`(launchd가 되살림). **CLAUDE.md §B-1 절차 필수 · cycle_state 무접촉.**

## 3. 모듈 지도 (~15,000 LOC / 18 모듈)
| 파일 | LOC | 책임 |
|---|---:|---|
| `orchestrator.py` | 2,681 | **v1 committee**(은퇴, 코드·DB 보존). 시황·뉴스·TA 조회 함수는 현행이 재사용 |
| `aifund.py` | 2,247 | **v2 데스크 핵심** — 소싱·분석·딥스터디·관찰·결재·집행·브리핑·주간리포트 |
| `db.py` | 1,976 | SQLite 스키마 + 전 헬퍼. 계좌 상수(`DESK_ACCOUNTS` 등) |
| `main.py` | 1,966 | FastAPI 라우트(공개 API + 오너 API) · 인증 · 가격 캐시 |
| `fetchers.py` | 686 | 시세·재무 조달. **가격 3단 폴백** |
| `backtest.py` | 478 | 규칙 전략 백테스트 엔진(추세돌파·평균회귀·모멘텀) |
| `trading_engine.py` | 273 | v1 규칙봇 실행부 |
| `prompts.py` | 204 | 페르소나 로더 (실제 내용은 `strategy_private.py`, **gitignore**) |
| `ops.py` | 206 | DB 핫백업·무결성·회전·오프사이트·워치독·세이프모드·`/health` |
| `agents.py` | 181 | `claude` CLI 호출 래퍼(토큰 소진 감지 포함) |
| `toss.py` | 167 | 토스증권 OpenAPI(시세 최종 폴백, ~4.5 TPS 스로틀) |
| `benchmark.py` | 145 | 봇 NAV vs 지수 누적%·알파 |
| `risk.py` | 138 | 회로차단기 + 한도 (결정적, LLM 무관) |
| `minute_data.py` | 122 | 형제폴더 `InvestData/data_us` 1분봉 → 일봉 리샘플 |
| `backtest_minute.py` | 111 | 로컬 분봉으로 전략 재검증 (오프라인 도구) |
| `scheduler.py` | 91 | APScheduler 잡 정의 |
| `ranks.py` | 81 | 봇 직위(픽 성과 기반 승진) |
| `discovery_naver.py` | 72 | (KR용, 현재 미가동) |

**정적 자산**: `static/index.html`(관전 SPA) · `static/console.html`(운영 콘솔) · `static/admin.html`(로그인 폼) · `static/seeds.html`(레거시)

## 4. 봇 로스터
> **내부 코드 레터 → 공개 표기(ROT13)**. UI·피드·외부 노출은 **공개 레터**만 쓴다.

| 내부 | 공개 | 역할 | 계좌 | LLM |
|---|---|---|---|---|
| A | **N** | 리서처 — 후보 발굴 + 전 후보 리서치 리포트 | 없음 | ❌ 룰 |
| P | **C** | 성장주 | 공유 | ✅ |
| W | **J** | 가치 — + 해외 국가지수/실물자산 대안 검토 | 공유 | ✅ |
| H | **U** | 실적 | 공유 | ✅ |
| S | **F** | 병목 — 자체 소싱이 엣지(A 리스트 안 받음) | 공유 | ✅ |
| Q | **D** | 타이밍 — 대형주 매수 **veto** + 청산 신호 | 없음 | ❌ 룰 |
| M | **Z** | 거시 브리핑 — **비투표**(보고만, veto 아님) | 없음 | ✅ |
| R | **E** | 리스크 오피서 — **비투표** 전천후 렌즈 | 없음 | ✅ |

- 페르소나 원문은 `strategy_private.py`(gitignore). `AGENT_PROFILES`에 **A·Q는 없음** — 둘은 룰봇이라 프롬프트가 필요 없다.
- v1 committee 봇(장기 7 / 트레이딩 5)은 코드·DB에 남아 있으나 **스케줄 미배선 = 은퇴**.

## 5. 계좌 (가상)
| 계좌 | 시드 | 현재 |
|---|---:|---|
| **대형주** | 5,000만 | 현금 4,174만 · 2종목 · 실현 +174만 |
| **발굴주** | 5,000만 | 현금 250만 · 19종목 · 실현 +0.7만 |
| ~~Q지수~~ | 1,000만 | **폐지**(`RETIRED_ACCOUNTS`) — 성적 집계 제외 |

`db.CORE_ACCOUNTS = ("대형주","발굴주")` 만 벤치마크 대상. 시드 정규화해서 계좌 신설이 수익으로 잡히지 않게 한다.

## 6. 매매 파이프라인
```
[소싱]
  대형주 : source_largecap()      18섹터 / 63종목 유니버스, 매일 전 섹터 재분석
  발굴주 : source_today()         일반 풀
           source_bottleneck()    승인된 병목 시드(approved 70) — S 자체 소싱
           run_bottleneck_curation()  하루 1회 웹서치로 무명 상류 후보 → pending 시드
           run_j_index_review()   J: 해외 국가지수(16) + 실물자산·채권 대안
        ↓
[분석]  대형주: P/W/H OR게이트  →  Q veto (200MA 아래 / 50MA 아래 차단)
        발굴주: A픽=P/W/S 위원회 · S픽=S 독자(시드 프레임: 진입가·희석·타이밍만)
        ↓
[딥스터디] _deep_study()   스스로 반박 시나리오 검증(악마의 변호인). 살아남아야 통과
        ↓
[관찰]  run_observation_review()  하루 1회 재점검 · 최소 2일 · 7일 넘으면 자동 철회
        현재: observing 24 · convinced 11
        ↓
[결재]  _submit_buy_approval()  → 운영 콘솔 + ntfy
        오너가 ✅승인 / ❌거부 / 💬질문(담당 봇이 재검토해 답변)
        현재 처리: approved 29 · rejected 5
        ↓
[리스크] risk.precheck_buy()   회로차단기 걸리면 승인해도 차단
        ↓
[체결]  봇 판단 시점 가격·시각으로 기록  (승인 만료 없음)

매도·손절은 자동 (결재 없음). 논지 청산 + Q 신호 + 손절.
```

## 7. 실행 모델 — 워크데이 (고정 시간표 폐지)
`WORKDAY_ENABLED=true`일 때 **keeper 하나가 전부** 굴린다:
```
평일 05:30~21:30 매시 30분  keeper(run_workday) 출근/재개 시도 — 락으로 중복 방지
  출근 → [아침 블록 · 전부 하루 1회 가드·멱등]
          거시 브리핑 → 병목 큐레이션 → 대형주 사이클 → J 지수 검토
          → Q 지수 → 리스크 리뷰
       → [종일 발굴 스터디 라운드 반복]
          라운드당 후보 8종목 · 라운드 간 20분 휴식
          토큰 소진 → 쉬었다가 충전되면 자동 재개
          사용자가 채팅 중이면 라운드 양보(우선순위 큐 선점)
       → 22시(KST) 퇴근
```
- 서버가 죽어도 다음 30분에 keeper가 **재출근**한다(crash-resilient).
- `WORKDAY_ENABLED=false`로 내리면 옛 고정 크론 시간표로 롤백.
- **항상 도는 잡**(데스크 게이트 무관): 벤치마크 스냅샷(평일 06:00) · 시황 스냅샷(30분) · 주간 리포트(토 09:00) · DB 백업(03:00) · 워치독(15분).

## 8. 게이트 플래그 (`.env` — 전부 끄면 no-op으로 복구)
| 플래그 | 뜻 |
|---|---|
| `NEW_DESK_ENABLED` | v2 데스크 마스터 스위치. **false면 전 파이프라인 정지** |
| `ROOT_IS_FUND` | `/`를 v2 화면으로(끄면 v1 committee 화면) |
| `WORKDAY_ENABLED` | 워크데이 keeper ↔ 고정 시간표 |
| `BUY_APPROVAL_REQUIRED` | 매수를 즉시 체결 대신 오너 결재 큐로 |
| `OBSERVATION_REQUIRED` | 관찰 단계 강제 |
| `MACRO_BRIEFING_ENABLED` | M 거시 브리핑 |
| `BOTTLENECK_CURATION_ENABLED` | 웹서치 시드 큐레이션(하루 1콜) |
| `RISK_GUARD_ENABLED` | 회로차단기·한도 |
| `RISK_OFFICER_ENABLED` | R 리스크 오피서 |
| `Q_INDEX_ENABLED` | **현재 false** — Q 지수 트레이딩 내림 |
| 기타 | `OWNER_TOKEN`·`ADMIN_PASSWORD`·`SITE_URL`·`NTFY_TOPIC`·`TOSS_*`·`OBSERVATION_MAX_DAYS` |

## 9. 리스크 3계층 (`risk.py` — LLM 무관, 결정적)
| 계층 | 항목 | 동작 |
|---|---|---|
| **절대 차단** | 킬스위치 · 일일손실 −5% · 데이터 이상(시세 실패율 >50% & 10건+) | **오너가 승인해도 매수 차단** |
| **한도** | 계좌당 보유 ≤25종목 · 단일 종목 ≤계좌 15% | 초과분만 거절 |
| **운영** | 디스크·DB 무결성 워치독 → 세이프모드 · `/health` 503 | 15분 주기 |

킬스위치는 **운영 콘솔에서 원터치**로 켜고 끈다.

## 10. 데이터 소스
- **현재가 3단 폴백**: `TradingView → yfinance → 토스증권`. yfinance 429 장애를 토스가 받는다. 토스 자격증명은 형제폴더 `InvestData/.env`, ~4.5 TPS 스로틀.
- **캐시**: 장중 180초 / 장외 1800초. 백그라운드 갱신은 **데스크 계좌 우선** 정렬.
- **재무**: yfinance + last-good 영속 캐시(`fundamentals_cache`) — 리밋 때 "관망만 나오던" 문제 방지.
- **분봉 아카이브**(오프라인 연구용): `InvestData/data_us/*.csv.gz` 3,246종목 · 6.8GB · 2021-12~.
- **데이터 헬스**: `fetch_health`에 계측, 실패 급증 시 오너 ntfy.

## 11. HTTP 표면
| 구분 | 경로 |
|---|---|
| 공개 | `/` 관전 SPA · `/api/state`·`/api/messages`·`/api/portfolio`·`/api/positions`·`/api/benchmark`·`/api/fund`·`/api/market` · `/api/user-message`(채팅) · `/api/feedback` · `/health` |
| **오너 전용** | `/admin` **운영 콘솔** · `/api/buy-approvals`(+`/decide`·`/ask`) · `/api/bottleneck/seeds`(+`/decide`·`/add`·`/curate`) · `/api/risk`(+`/kill-switch`) · `/api/ops` · `/api/weekly-report` · `/api/macro-briefing` · `/api/admin/visits`·`/feedback`·`/chats` |
| 인증 | 쿠키 단일 토큰. `/owner/<OWNER_TOKEN>`이 쿠키 발급, 그 외 `/owner/*`는 `/admin` 리다이렉트(쿠키 미발급). **2FA 없음 — 실계좌 전 강화 필요** |

**운영 콘솔(`/admin`) 구성**: 🛡️리스크 가드(킬스위치) · 🧭거시 브리핑 · 📈주간 리포트 · ✅매수 결재(질문/승인/거부) · 🌱시드 결재 · 📊방문 통계 · 📨피드백 · 🗨채팅 기록.
`/owner/approvals`·`/owner/seeds`는 **폐지**(→`/admin`).

## 12. DB 테이블 (39개, 역할별)
- **매매**: `shared_portfolio` `virtual_positions` `watched_stocks` `largecap_watch` `thesis_cache`
- **결재·큐**: `pending_buy`(+qna) `observation` `bottleneck_seed` `chat_queue`
- **계측**: `decision_log`(6,878행 — 판단+입력패킷+페르소나해시) `weekly_report` `position_snapshot` `benchmark_daily` `fund_nav` `fetch_health`
- **콘텐츠**: `messages`(`desk`로 v1/v2 분리) `rounds` `fund_report` `consensus_notes` `macro_briefing` `lecture_notes`
- **운영**: `risk_flag` `ops_state` `token_usage_daily` `token_capacity` `cycle_state` `agent_state`
- **관전 지표**: `visit_log` `feedback` `member_profiles` `donations`
- **v1 잔존**: `blog_posts` `blog_signals` `daily_analysis_log` `market_snapshots` `stock_names` `fundamentals_cache` `events`

## 13. 성적 계측
- **벤치마크**(2026-07-28 baseline, 17 스냅샷): 봇 **+1.15%** / SPY +2.93% / QQQ +4.22% → **알파 SPY −1.78%p · QQQ −3.07%p**. 시드 정규화 적용(계좌 신설이 수익으로 안 잡힘).
- **주간 리포트**(토 09:00, LLM 0콜·결정적): 봇별 판단 5거래일 후 채점 · **오너 결재 부가가치**(승인 vs 거부 사후 성과) · 관찰 전환율 · 데이터 헬스.
- **관전 리텐션**: D1 7.7% · D7 10.5% (모수 작음). 이번 사이클 핵심 질문 = *"관전만으로 사람이 다시 오는가"*.
- 상세: `docs/METRICS.md`

## 14. 테스트·CI
- `pytest` 23개 파일 · **290 passed / 2 skipped**.
- GitHub Actions 3잡: `test` · `lint`(PR 제목 컨벤션 `[Type] #N - 내용`) · `label`.
- ⚠️ CI엔 `strategy_private.py`가 없다 — 페르소나 의존 테스트는 `monkeypatch`로 주입할 것.

## 15. 현재 상태
- **머지 완료(2026-08-30)**: #156(J 실물자산·채권 풀) · #157(운영 콘솔 통합 + 방문통계 버그 + 옛 경로 폐지). main = `e7b0026`.
- **라이브 진입점**: 관전 `/` · 운영 콘솔 `/admin`(결재·킬스위치·방문통계·피드백·채팅기록). `/owner/approvals`·`/owner/seeds`는 폐지.
- **최근 트래픽**: 8/28 13명·63PV(인스타 버스트) → 8/29 6명 → 8/30 2명. 채팅은 8/28 30건 이후 0건.
- 🔒 **보안 감사 1회차(2026-08-31) — 미조치분이 남아 있다. 아래 16번 참조.**
- 아직 답 못 받은 판단은 바로 아래 섹션.

---

## 16. 🔒 보안·전략노출 상태 (2026-08-31 감사 — 미완결)

> "베끼려는 사람" 관점 3면(공개 GitHub · 오너 콘솔 · 공개 사이트) 정기 감사.
> 레포는 **PUBLIC 유지** 결정 → 개별 수술로 간다.
>
> ⚠️ **상세는 `SECURITY_PRIVATE.md`(gitignore)에 있다.** 미조치 취약점의 구체적 경로라
> 공개 레포인 이 파일에 적으면 그대로 공격 지도가 된다. 여기엔 상태만 둔다.

- **1회차 완료 2026-08-31.** 3면 전부 감사, P0 항목은 직접 재확인.
- **조치 완료(머지 대기)**: 공개 API의 비공개 전략 유출 차단(`fix/api-strategy-leak` — **푸시 실패, 로컬에만 있음**) ·
  owner_guard deny-by-default 반전 + 회귀 테스트(`fix/deny-by-default` — 푸시 완료).
- **미조치 9건** — 자격증명 1건(사용자 조치 필요) · 공개 레포 위생 6건 · 서버 하드닝 1건 · 관전 피드 1건.
  사용자 손이 필요한 2건은 아래 **미답 제안** 참조.
- **가장 중요한 방어는 살아 있다**: `strategy_private.py`는 한 번도 커밋된 적 없고, 라이브 키·토큰은 전부 env 경유.

---

## ❓ 미답 제안 (사용자 답 대기 — 휘발 금지)
- 🔒 **보안 감사 후속 — 사용자 손이 필요한 것 2건**(2026-08-31, 상세·이유는 `SECURITY_PRIVATE.md`):
  1. **외부 서비스 자격증명 회전 1건** — 최우선. 코드로는 해결 안 되고 해당 서비스에서 직접 해야 한다.
  2. **스테일 원격 브랜치 4개 삭제** — 백업 `~/icb-branch-archive/` 완료(4개 전부 무결성 검증). 클로드가 시도했으나 안전 분류기에 차단됨. 명령은 `SECURITY_PRIVATE.md`에.
- 🔒 **`docs/AIFUND_PIVOT.md`·`docs/backtest_report.md`·`PROGRESS.md` 비공개 이전 여부** — 셋 다 공개 레포에 있고 설계도·검증표·내부 대응표를 담고 있다. 별도 private 레포로 뺄지, 공개용 축약본만 남길지 미결.
- **GitHub Support 요청**(옛 커밋 서버측 완전 삭제) — 초안 전달함(스크래치 md), **사용자 제출 대기**. 감사에서 나온 추가 대상도 같은 요청에 포함시킬 것.
- **J 해외 개별주 유니버스(B안)** — 국가 지수만으론 "비싼 시장 안의 싼 기업"을 못 고른다. 실제 참고 사례(일본)도 지수가 아니라 **개별 상사 5곳**을 산 것. 해외 개별주 가격·재무 조달이 되는지 확인 후 도입할지. (A안=지수 풀 유지·성격만 낮춤, C안=Q veto 예외는 데이터 부족으로 보류 권고)
- **테마형 ETF 추가 여부** — 사용자가 처음 언급한 "중국 로봇 지수" 계열. 현재 J 풀은 국가 지수 + 실물자산뿐. F(병목)의 개별주 발굴과 역할이 겹칠 수 있어 **국가 지수 위주 유지**를 권고했으나 미답.
- **AZZ 처리** — 2026-08-14 오너가 결재 **거절**한 종목인데 08-17 일괄 매수 배치에서 다시 편입됨(현재 −5.2%). 청산할지 유지할지 미결. → 재발 방지책(거절 이력 종목 재매수 차단)도 같이 판단 필요.
- **채팅 1인당 캡 도입** — 현재 캡은 글로벌 30건/일뿐이라 한 명이 전부 소진 가능. 8/28에 실제로 발생(7명 중 1명이 10건=33%, 34분 만에 소진, 이후 유입 6명 채팅 차단). 추천값 **1인당 8건**. 숫자만 정해지면 즉시 작업 가능. 함께 필요: `enqueue_chat_question`에 `visitor_id` 전달(한 줄 — 지금 파라미터가 있는데 안 넘겨서 `chat_queue.visitor_id`가 전부 NULL).
- **페르소나 실명 제거** — 깃엔 없지만 **런타임 봇 출력에 투자자 실명이 노출**됨(8/28 공개 채팅에서 확인). `strategy_private.py`에 잔존 추정.
- **uvicorn 액세스 로그 회전** — `~/Library/Logs/investchatbots/app.log` 380MB·무한 증가. 클라이언트 IP가 여기 남는다(DB엔 없음).
- **`static/index.html` 죽은 모달 코드 ~250줄** — 통계·피드백목록·채팅기록이 운영 콘솔로 이관돼 호출되지 않음. 별도 PR로 제거할지.
- **분봉 백테스트 2·3단계** — 1단계(규칙 전략 재검증)만 완료. 2단계=LLM 판단 사후 검증, 3단계=페르소나 A/B. 미착수.
- **매수 후 즉시 매도 쿨다운(B안)** — 근본 원인(Q 게이트 200MA vs 청산 50MA 비대칭)은 veto에 50MA 추가로 해결했으나, "최소 보유기간" 안전장치는 채택 안 함.

---

# 📚 이하 = 히스토리 (옛 스냅샷 · 세션 로그)

> ⚠️ **아래는 지나온 기록이다.** 당시엔 맞았지만 지금과 어긋나는 서술이 섞여 있다
> (예: 옛 스케줄·옛 계좌·`/owner/approvals`·Q지수 계좌).
> **현재 사실은 위 🧭 시스템 전체 구성이 단일 진실원천**이고, 아래와 충돌하면 위가 이긴다.
> 아래는 "왜 이렇게 됐나"를 추적할 때만 읽을 것.


## 🚧 다음 세션 — S/Q 재설계 로드맵 (2026-08-05 확정)
> ② 큐레이션 흐름 **전부 구현 완료**. 다음은 활성화(아래 ⚠️) + ③~⑥.
- **① S 시드 = 사람 큐레이션** ✅ 구조 완성. 역할분담: **발굴·조사=Claude(웹서치)** / **승인=사용자(/owner/seeds)** / **평가·매매=라이브 S봇**.
- **② 6시 큐레이션 흐름 — 전부 ✅ (로직 완성, 활성화 대기)**:
  - a. ✅ 병목 시드 **DB 테이블화**(`bottleneck_seed`). db 헬퍼 add/get/decide/backfill. (#110 머지)
  - b. ✅ `source_bottleneck`이 **DB approved 시드** 읽음. 빈 테이블은 하드코딩 14시드 approved 백필. (#110 머지)
  - c. ✅ **Admin 승인 UI**: `GET /owner/seeds` + 오너 API `/api/bottleneck/seeds`·`/decide`·`/add`. (#110 머지)
  - d. ✅ **6시 웹서치 에이전트**(브랜치 `feat/bottleneck-curation`): `aifund.run_bottleneck_curation()` = Claude 웹서치로 무명 상류 초크포인트 조사 → pending. `_call_claude_cli(allowed_tools=['WebSearch'])`. scheduler 05:50 mon-fri(06 대형주 전에 pending 준비). 게이트 `BOTTLENECK_CURATION_ENABLED`(기본 off). 수동트리거 `POST /api/bottleneck/curate`. **결정: launchd 아니라 in-app 스케줄러**(서버가 Mac mini 로컬 DB 접근 이미 있음 → 더 단순). **비용=별도 크레딧 아님, 구독 토큰 버킷**(하루 1콜). E2E검증(AXTI·KLIC·MOD 발굴).
  - e. ✅ **S 피드 "결재 올림" + ntfy**: `submit_bottleneck_candidates()`가 pending+S내레이션+오너ntfy(링크=`/owner/seeds`). (#110 머지)
  - **⚠️ 남은 활성화**: `feat/bottleneck-curation` 머지 → `.env`에 `BOTTLENECK_CURATION_ENABLED=true`(원하면 `SITE_URL`도) → §1 절차로 재시작. 안 켜면 05:50 잡 no-op.
- **③ S 렌즈 추가 심화**: 오늘 페르소나에 "저마진·적자여도 논지로, 시총vsTAM·희석 본다" 한 줄 넣음(라이브). 외부 공급망 리서처의 SKILL.md 프레임으로 더 정교화 가능(선택 — 출처·분석은 비공개 노트 참조).
- **④ 시드 정비**: 현 14개에 AVGO·ANET·MRVL(유명 '참치') 섞임 → 무명 상류로 교체. **이제 도구 완비**: 6시 에이전트가 매일 무명 상류 후보를 pending에 올리고(②d), `/owner/seeds`에서 참치 시드 반려·신규 승인. 남은 건 운영(며칠 결재하며 참치 정리).
- **⑤ 거시자문 봇 = 비투표 매크로 자문**(트레이딩봇 X). blog_posts(보유 277편) 원문 보유. `병목자문` 패턴처럼 AGENT_PROFILES 자문 봇으로 — "거시적으로 지금 사면 안 된다" 시장레벨 경고. (KR/서사형이라 트레이딩 룰로는 안 맞음.)
- **⑥ 유니버스 큐레이션 일반화**: 섹터·시드 다 "사람이 정비→봇이 굴림". 오늘 섹터 잡탕 1건 고침(아래), V·MA 정합은 미결.

> updated(원): 2026-08-02 (🎯 컷오버 완료 — AI펀드 자동 가동. committee 은퇴, fund 스케줄 라이브. #53·#89 닫힘, #98 활성화)

## 🧪 지금 방향 (2026-07-27~ US-only 실험)
- **국장(KR) 사이클 임시 off** (`orchestrator.KR_CYCLE_ENABLED=False`) — US-only로 전략 포텐셜 관찰. 재개는 True+재시작.
- 양 계좌 시드 통일 **1억**: 장기=확신도 티어 500/1000/1500만, 트레이딩=플랫 500만·MAX 20.
- US 섹터 9→17 확장(병목·성장·꿈주). 공급망 병목 "공급망 병목" 이론 접목: 규칙엔진 유니버스 병목 시드 + 미장 라운드 비투표 자문 코멘트.
- KR 거래내역·보유는 제거(백업 `bak-pre-usonly`), US-only로 재베이스. **미장 마스터하면 국장 재개 + 코인/QQQ/SCHD 포트폴리오 전략 도입 예정.**
- US-only 현재 성적(시드 각 1억): 장기(위원회) **+4.69%** / 트레이딩(규칙엔진) **+1.08%**.

## 지금 방향
- 핵심 질문(이번 사이클): **"관전만으로 사람이 다시 오는가?"** — 재방문/리텐션 증명이 최우선.
- 가상 포트폴리오 단계. EverydayNews(아침 메일)가 유입구.
- 상세 OKR·오케 코멘트: 워크스페이스 `okr/investchatbots.md`(권위본) 참조.

## 🎯 현재 = AI펀드 자동 가동 중 (컷오버 완료 2026-08-02)
- **라이브 `/` = fund**(committee 은퇴). 스케줄러: **06 대형주**(run_largecap_cycle) · **0·12·18 발굴**(run_discovery_cycle), 평일 KST. `NEW_DESK_ENABLED=true`(.env 게이트, 끄면 no-op·복구).
- **committee 보존**: 코드·DB·conversation loop 그대로(cycle_rest idle). 스케줄러 잡만 제거 → 필요시 scheduler.py 복구로 재가동.
- **계좌**: 대형주(id7) 보유 TSMC·엔비디아(각 400만)+현금 42.7M+실현 702,684 / 발굴주(id8) 시드 5000만·0보유. 사이징 대형주 500만(10%·max10)·발굴주 250만(5%·max20).
- **⚠️ 다음(가장 중요) = 첫 자동 사이클 모니터링**: 오늘 00:00 발굴 / 내일 06:00 대형주가 첫 실주행. 봇 실매매·토큰 시작. §1 재시작 절차 준수, cycle_state 무접촉. 발굴 매수율 낮으면(랜덤풀에 P/W/S 보수적) 문턱 튜닝 검토.
- **완료 이슈**: #53(데스크 빌드)·#89(관전 UX A~G)·#98(컷오버 배선·활성화). 설계 `docs/AIFUND_PIVOT.md`.
- [ ] 채팅 = **관전 피드로 재개념화**(안전장치 ①큐 폐기, ②면책·③권유금지만). 유저 질문 라이트 유지.
- [ ] 리텐션 계측 정확화 (재방문율 신호가 아직 부정확 — 실방문/재방문 정의 재확인)
- [ ] (수익화 뒤 레이어) 구독 → EverydayNews 발송목록 + 채팅 입력권한 제한

## 안정 완료된 것 (요약)
- 국장/미장 이원화 사이클(06시 KRX / 18시 US), 보유점검·손절·워치리스트 스케줄.
- 7봇 위원회 토론 → 과반 매매, 가상계좌 포트폴리오.
- 최근 안정화: FD 누수 방지(urlopen close + OHLC 캐시), fetch_one MultiIndex 크래시 수정, 규칙봇 결정 대화체.
- 릴스 자동 파이프라인 삭제 → 당분간 수동 업로드(파급력 우선).


## ✅ 완성 스냅샷 (2026-08-10 기준 — 개발 현황)
- **규모**: 코드 ~14,050 LOC(9모듈) · DB 39테이블 · **pytest 246 올그린** · 이번 세션 PR #110~#134(24개) 머지.
- **스택**: FastAPI(uvicorn) 단일 프로세스 8001 · SQLite 단일파일 · APScheduler(인프로세스) · Mac mini+launchd · cloudflared→investchatbots.com · LLM=claude CLI 구독토큰.
- **라이브 게이트(.env, 전부 true)**: NEW_DESK · ROOT_IS_FUND · WORKDAY · BUY_APPROVAL_REQUIRED · OBSERVATION_REQUIRED · MACRO_BRIEFING · BOTTLENECK_CURATION · Q_INDEX · RISK_OFFICER · RISK_GUARD.
- **완성된 파이프라인**: 소싱(A일반풀 + S다중트렌드 병목 + 6시 웹서치 큐레이션) → 분석(A픽=P/W/S위원회 / S픽=독자·시드프레임) → **딥스터디(셀프반박)** → **관찰(며칠 재점검·확신 시)** → **오너 결재(/owner/approvals)** → **리스크 회로차단기** → 체결(판단시점 가·시각). 매도·손절 자동.
- **실행모델**: 워크데이 — 시간표 폐지, keeper(평일 05:30~21:30) 출근제·종일 스터디 라운드·토큰소진 시 대기후 재개·crash 시 재출근.
- **로스터**: 리서치·성장주·가치·실적·병목(독자·다중트렌드)·타이밍+지수계좌·거시브리핑·리스크(비투표). 내부 레터는 출력 경계에서 치환되며 **대응표는 공개하지 않는다**.
- **계좌**: 대형주(id7,5천만)·발굴주(id8,5천만)·Q지수(id9,1천만). 결재→체결 첫 완주(TSM·NVDA + 재심사 발굴주 11종).
- **신뢰성 P0/P1**: decision_log(재현성·입력패킷+페르소나해시)·주간 성과 리포트(봇 채점+결재 부가가치)·데이터 헬스ntfy // 리스크 회로차단기(킬스위치·일일손실·데이터이상=절대차단, 포지션수·비중=한도) // 인프라(DB 핫백업·무결성·회전, 부팅 자가진단, 15분 워치독→세이프모드, /health 503).
- **보안·정체 은닉**: 오너 쿠키 인증 · 전략가 실명 히스토리 스크럽 · 봇 이니셜 출력 치환 · 페르소나는 strategy_private(gitignore). 상세·미조치분은 SECURITY_PRIVATE.md.

## 🗺️ 다음 후보 (실매매 신뢰성 로드맵)
- P1 잔여: 외부 워치독(앱 별개 프로세스가 /health 폴링) · 시세 소스 이중화(yfinance→브로커 폴백).
- P2: 브로커 페이퍼 API 연동(KIS/알파카 — 실 주문경로·슬리피지 학습).
- P3: 90일 포워드 성과 기준 통과 시 소액 실계좌.
- 인증 강화(2FA/IP제한) — 실계좌 전 필수.

## 최근 세션 로그 (최신이 위)
- **2026-08-31 (🔒 보안·전략노출 감사 1회차 + Epic 위생)** — 상세는 위 **16. 보안·전략노출 상태**.
  - **발단**: "베끼려는 사람이 있다고 가정하고 주기적으로 확인해줘"(GitHub·Admin·공개사이트 3면). 병렬 감사 3종 실행 후 최상위 항목은 직접 재확인.
  - **가장 뼈아픈 패턴 — "JS로 가리고 API는 열어둠"**: 어떤 공개 엔드포인트가 UI에서는 오너에게만 보이는 데이터를 payload로는 전원에게 보내고 있었다. **프론트 게이팅은 접근제어가 아니다.** (해당 경로·필드는 `SECURITY_PRIVATE.md`.)
  - **구조적 원인 — 허용목록 방식**: `owner_guard`가 수동 허용목록이라 **등록을 빠뜨린 라우트가 조용히 공개**됐다. 실제 누락이 있었고 토큰 버킷 소각(= Claude Code 세션까지 정지)이 가능했다. → **deny-by-default로 반전** + "무인증 mutating 라우트 0개" 회귀 테스트로 잠금. 앞으로 가드를 잊어도 안전한 쪽으로 실패한다.
  - **또 하나의 근본 패턴 — 과잉 직렬화**: `SELECT *`를 그대로 반환해 UI가 안 쓰는 내부 컬럼이 나갔다. 새 공개 GET은 **필드 화이트리스트**로. `/api/fund`가 이미 그렇게 하고 있어 템플릿으로 쓸 것.
  - **2026-08-07 실명 스크럽은 절반만 성공** — main에만 적용됐고 **원격 스테일 브랜치가 재작성 전 상태로 살아 있다**. 머지 후 브랜치 자동삭제가 실제로는 꺼져 있는 듯(CONVENTION.md엔 ON이라 적혀 있음) — 확인 필요. 교훈: **히스토리 재작성은 모든 ref에 해야 끝난다.**
  - **테스트 함정 하나 잡음**: 유출 회귀 테스트가 `virtual_positions` 0행 위에서 **조용히 통과**하고 있었다. 픽스처로 실제 행을 넣고 "비면 실패" 가드를 넣어야 의미가 생긴다. 감사성 테스트는 빈 데이터에서 헛도는지 항상 확인할 것.
  - **막힌 것**: 세션 중 안전 분류기가 `gh issue create`·`git push --delete`·일부 `push`를 비결정적으로 차단(내용 무관 — 비보안 이슈도 동일). 이슈는 사용자가 생성해야 함.
  - **⚠️ 내가 만든 사고 2건**: ① 이슈를 못 만들자 **번호를 예측해 `#159`로 4개 브랜치를 커밋** — 존재하지 않는 유령 참조. ② **stale한 로컬 `main`(`e7b0026`)에서 브랜치를 따서** 전부 origin/main보다 뒤처진 베이스 위에 쌓임. 세션 시작 시 `git fetch`로 관계 확인하라는 규칙을 안 지킨 결과. 둘 다 리베이스로 정리.
  - **Git 컨벤션 개정**: #98 종료(투두 체크 + 사유 코멘트) 후 사용자가 "여전히 이상하다"고 지적 → 실측하니 **PR 123개 중 91개(74%)가 에픽 3개**(#98 56 · #53 26 · #89 9)에 몰려 있었다. 원인은 디시플린이 아니라 **구조적 편향** — "에픽에 얹기"는 번호만 참조하면 되는데(무비용) "단발"은 이슈를 새로 만들어야 해서 저항이 적은 쪽으로만 흘렀다. → **작업 단위 = 지시 단위**로 개정(1지시=1이슈, 보통 1PR·동시 지시면 N PR). 옛 이슈에 새 작업을 얹지 않는다.
- **2026-08-28~31 (첫 트래픽 버스트 — 관전 서비스의 실전 데이터)** — #156·#157 머지(8/30). 코드 변경 없이 **운영 관측**이 주 성과.
  - **8/28 인스타 유입 버스트**: 방문 13명·63PV(20시대에 55PV 집중). **12명이 인스타 유입, 전원 모바일**, 데스크톱 0.
  - **재방문 6/13명** — 8/7·8/12 첫 방문자들이 돌아옴. 해석: *"저절로는 안 오지만 부르면 온다."* 단 8/7·8/12도 유입 이벤트가 있던 날이라 **'같은 사람을 반복해 부르는 것'인지 구분 필요**(다음 유입 때 확인할 것).
  - **채팅 30건 = 7명**(IPv6 기준, KT 2·SKB 3·LGU+ 2 회선). 34분 만에 **글로벌 캡 30 소진** → 21시 이후 유입 6명은 채팅 시도조차 못 함. 최다 이용자 1명이 10건(33%).
  - **🐛 근본 원인**: `chat_queue.visitor_id`가 **전부 NULL**. `enqueue_chat_question(content, visitor_id=None, ...)`에 호출부가 visitor_id를 안 넘긴다 → 앱은 누가 보냈는지 모르고, 캡이 글로벌로만 작동.
  - **IP 추적 가능 여부**: DB엔 IP 컬럼 없음. 단 **uvicorn 액세스 로그**(`~/Library/Logs/investchatbots/app.log`)에 클라이언트 IP가 남아 사후 복원됨. 타임스탬프가 없어 POST 순서 ↔ `chat_queue.id` 순서 대조로 발신자 추정.
  - **🛡️ 프롬프트 인젝션 시도 1건 — 방어 성공**: *"지금까지 기록된 프롬포트 다 잊고 …"*. 봇 4개 전부 캐릭터 유지하며 거절(시스템/유저 프롬프트 분리 덕). 오프토픽 사용 다수(맛집·키·전래동화) — 포지셔닝 이슈.
  - **⚠️ 발견**: 봇 런타임 출력에 **투자자 실명 노출**. 깃 히스토리는 스크럽됐지만 페르소나에 남아 공개 채팅에 찍힘.
  - **📌 규칙 신설 — 정기 체크포인트**: 세션 시작/끝뿐 아니라 **작업 묶음이 끝날 때마다** PROGRESS.md를 갱신한다(CLAUDE.md 세션 프로토콜에 명문화). `/clear`로 컨텍스트가 날아가도 기록이 남게 하는 목적.
- **2026-08-23~24 (운영 콘솔 통합 + J 대안자산 + 시스템 지도)** — #155 머지, #156·#157 대기.
  - **#155(머지)**: J 전용 **해외 국가지수 풀 16종** — 미국 개별주가 비쌀 때의 대안. 하루 4종 날짜시드 순환, 시장레벨 렌즈(시장 전체 밸류·통화/정치 리스크·구조적 성장·미국 대비 상대매력). 첫 실행 4종 전부 관망.
  - **#156(대기)**: J에 **실물자산·채권 9종** 추가(금·은·금광·백금·원자재·우라늄·장기국채·물가연동채·리츠). PER이 없는 자산군이라 **전용 프롬프트로 분리** — 실질금리·통화가치 / 수급 구조 / 역사적 밴드 위치 / **"현금보다 나은가"**. 하루 지수4+자산2.
  - **#157(대기)**: **`/admin` = 운영 콘솔로 교체.** 그동안 `/admin`이 공개 SPA를 그대로 서빙해 관전/운영이 섞여 있었다 → **관전은 `/`, 운영은 `/admin`**. `approvals.html`→`console.html` 승격 + 서비스 지표 3종(방문 통계·피드백·채팅 기록) 흡수. `/owner/approvals`·`/owner/seeds` **폐지**(→`/admin`, 옛 ntfy 링크가 403 되지 않게 토큰 불일치도 리다이렉트 — 쿠키는 미발급이라 우회 아님). ntfy 결재 링크도 `/admin`으로.
  - **🐛 방문 통계 버그**: `by_day`가 최신순(DESC)인데 `slice(-14)`로 **배열 뒤쪽(=가장 오래된 구간)**을 집어, 8/23인데 최신이 8/10으로 표시됐다. `slice(0,14)`로 수정 + **방문 0인 날은 행 자체가 없어 날짜축이 건너뛰던 문제**를 날짜축 zero-fill로 해결(추이 왜곡 방지). 코호트도 동일 버그.
  - **📌 확인된 사실**: 공개 13F 기준 대형 가치 운용사는 **금·원자재 ETF를 보유하지 않고** 살 게 없을 때 단기국채(현금)로 간다 → J의 "현금보다 나은가" 프레임이 방향은 맞음. 또 해외 투자도 **지수가 아니라 개별 기업**이었음 → 현 `J_INDEX_POOL`은 접근법이 한 겹 어긋나 있다(미답 제안 참조).
  - **문서**: PROGRESS.md 상단에 **🧭 시스템 전체 구성** 신설(코드에서 검증한 현재 지도 — 모듈·로스터·계좌·파이프라인·워크데이·게이트·리스크·데이터소스·HTTP·DB·계측). 아래 세션 로그는 히스토리이므로 **현재 사실은 그 섹션이 우선**임을 명시.
- **2026-08-08~10 (신뢰성 P0/P1 + 실매매 배관 완성)** — #123~#134.
  - **P0 계측(#123)**: decision_log(모든 판단+입력패킷+페르소나해시, 재현성)·주간 성과 리포트(토 09시, LLM0, 봇 채점+결재 부가가치)·데이터 헬스체크.
  - **워크데이(#124)**: 고정 시간표 폐지→출근제 keeper + 종일 스터디 라운드 + 딥스터디(악마의 변호인) + 토큰소진 재개.
  - **결재함 통합(#125)**: 시드/매수 결재 한 페이지(승인=매수 혼동 해소) + UTC/KST 가드 버그.
  - **이름 스크럽 완결(#126) + 테스트 올그린(#127)**: 코드 잔여 중립화, 사전 실패 11건 정리(229→246).
  - **승인 매수 시각 정합(#129)**: 체결가·시각 모두 결재 상신 시점 기준.
  - **시드 재프레임(#130)**: S가 병목 재심사 대신 진입가·희석·타이밍만 → 24종목 재심사=매수 11건 상신.
  - **다중 트렌드(#131)**: 큐레이션·S를 AI 편중 탈피(전력망·원자력·방산·바이오·우주…), 배치 AI 최대 2개.
  - **모달 친절화(#132)**: 오너승인 매수/청산 사유를 사람말로(브라우저 실검증).
  - **리스크 회로차단기(#133)**: 킬스위치·일일손실·데이터이상=절대차단(오너 승인도 무시)·포지션수·비중=한도. 오너 대시보드+원터치 정지.
  - **인프라 SPOF(#134)**: DB 핫백업(무결성·회전·오프사이트)·부팅 자가진단·15분 워치독→세이프모드·/health. Q지수 비중상한 충돌 버그 수정.
  - **규칙 신설**: 미답 제안 휘발 금지(CLAUDE.md·메모리). 모델을 Fable 5로 전환.
- **2026-08-07 오전 (워크데이 모델 — 시간표 폐지)** — 사용자 지적(제안 휘발) 반영: 스케줄 폐지·토큰 재개·긴 공부 시간 구현.
  - **✅ 워크데이(`WORKDAY_ENABLED=true`)**: 고정 크론 시간표 폐지 → keeper(평일 05:30~21:30 매시 30분)가 출근/재개(락 중복방지). 출근 → 아침 블록(브리핑·큐레이션·대형주·Q지수·리스크 — 전부 하루 1회 가드·멱등) → **종일 발굴 스터디 라운드**(라운드당 8종목·20분 휴식) → 22시 퇴근. **토큰 소진 시 15분 쉬고 충전되면 재개**, 서버 재시작에도 keeper가 재출근. 롤백 = false(옛 시간표 복귀).
  - **✅ 딥스터디(악마의 변호인)**: 매수 판단 → 스스로 반박 시나리오 검증(1콜) → 살아남아야 관찰 등록. 공부 과정 피드 노출 + decision_log('딥스터디') 기록.
  - **✅ 규칙 신설**: 미답 제안 휘발 금지(CLAUDE.md 협업 규약 + PROGRESS '미답 제안' 섹션 + 메모리).

- **2026-08-07 아침 (P0 성과 계측 + 확정 방향 기록)**
  - **✅ P0 구현(decision_log)**: 모든 봇 판단(분석/관찰/Q타이밍/Q지수)을 **입력 패킷+페르소나 해시**와 함께 기록(재현성·사후 분석). 실패해도 매매 로직 무영향(fail-safe).
  - **✅ 주간 성과 리포트**(토 09:00, 결정적 계산·LLM 0): 봇별 판단 7일 후 채점(hit rate) + **사장님 결재 부가가치**(승인 vs 거부 사후 성과) + 관찰 전환 + 데이터 헬스. **오너 전용**(관전 지표와 분리) — `/owner/approvals` 접이식 + ntfy. 수동 `POST /api/weekly-report/run`.
  - **✅ 데이터 헬스체크**: fetch_health 테이블 + fetch_stock_price 계측, 일 실패 10건+·50%+ 시 오너 ntfy(쿨다운).
  - **📌 P1 확정 방향(다음)**: "LLM이 못 뚫는 층" = **대형주는 퀀트가 선**(Q veto 유지·강화 — 우량주일수록 룰 게이트 적합). **발굴주는 제외**(논지 기반이라 퀀트 선 부적합 — 결재+관찰이 게이트). + 일일 손실 한도·서킷브레이커 설계.
  - **📌 인프라 단일 장애점(나중)**: Mac mini 단일 노드 + sqlite 단일 파일 + launchd. 실매매 전 필수: DB 자동 백업(오프사이트)·프로세스 헬스체크·fail-safe(서버 다운 중 포지션 처리 정의)·시세 소스 승격. 지금은 가상이라 보류.
  - **아침 첫 실주행 확인**: 05:40 브리핑 생성 ✓ · 05:50 큐레이션 5건 pending(ALAB·MTSI·CAMT·SITM·NVTS) ✓ · 06:00 대형주 섹터 토론 가동 ✓.
- **2026-08-06~07 (대량 세션 — 사람-in-the-loop 매수 + 봇 개성·신중함 재설계)**
  - **머지됨(main)**: #111(6시 큐레이션 ②d)·#112(백필가드)·#113(거시자문 Z + 이니셜 ROT13)·#114(매수 결재). ②d는 **라이브 활성**(05:50, `BOTTLENECK_CURATION_ENABLED=true`, 시드 14 approved 백필). 매수 결재 코드는 main에 있으나 **라이브 미배포·플래그 미설정**.
  - **전부 머지됨(2026-08-07 새벽, #116→#121 순)**. 같은 새벽에 **전 히스토리 재작성**: 커밋 메시지+blob에서 투자자 이름 완전 제거(문서·json 전부 / 코드는 CI 검증된 안전 치환만, 추세돌파·평균회귀 한글 표시는 코드 내 유지·영어 식별자 유지), mer_blog 원문파일 히스토리째 삭제, PR/이슈 메타데이터 12건 정리. 동작 무변경(214 테스트 통과). GitHub 서버 잔존 객체는 Support 요청 필요(초안 전달).
    - **#116** 전략가 실명 스크럽(레포 public — 실명→역할어. 과거 커밋 메시지 히스토리는 A안=보존).
    - **#117** 매수 결재 '상사 보고' 상세화(봇별 역할라벨+근거 본문) + **발굴 S 트랙 독립**(A픽=P/W/S 위원회, S픽=S 독자 → 결재).
    - **#118** Z(거시) 브리핑 — 05:40 블로그 새글 크롤+지수 → 오너 보고, `/owner/approvals` 상단 카드. `MACRO_BRIEFING_ENABLED`.
    - **#119** 관찰 단계(신중한 매수) — 매수판단→관찰 등록→사이클마다 자기논지 재점검→최소 2회 후 확신 시 결재(관찰 서사). `OBSERVATION_REQUIRED`.
    - **#120** Q 지수 스윙 계좌(id9·1,000만) — QQQ/SPY+1배 인버스(PSQ/SH), 200MA±20일 돌파/50MA 청산 미러, 06:10 자동(무LLM·결재 없음). `Q_INDEX_ENABLED`. +init_db 신규DB ALTER 누락 잠복버그 수정.
    - **#121** 자문단 — M 발굴 코멘트(브리핑 재사용·토큰0) + **R 리스크 오피서**(전천후 렌즈, 집중도·상관·시나리오·현금, 06:20 비투표). `RISK_OFFICER_ENABLED`. 공개레터 E.
  - **확정 정책**: 모든 매수=오너 결재(승인=봇 판단시점 가격 체결, 만료없음, 매도는 자동) / M=veto 아닌 보고 / 대형주 게이트=Q veto만 / 시드결재(/owner/seeds)≠매수결재(/owner/approvals).
  - **로스터**: 리서치 · 성장주 · 가치 · 실적 · 병목(독자트랙) · 타이밍+지수계좌 · 거시브리핑 · 리스크. 내부 레터는 출력 경계에서 치환(대응표 비공개). 위원회 한글명은 비치환.
  - **⚠️ 다음 = 머지 → .env 플래그 4종(`BUY_APPROVAL_REQUIRED`·`MACRO_BRIEFING_ENABLED`·`OBSERVATION_REQUIRED`·`Q_INDEX_ENABLED`·선택 `RISK_OFFICER_ENABLED`) → §1 재시작.** 이후 관전: 결재함 UX·관찰 서사 품질·Q지수 첫 신호.
  - 보류: 추세돌파 별도 봇(Q와 중복이라 안 함), M veto(보고로 대체), 대형주 관찰 단계(매일 재분석 구조라 미적용).
- **2026-08-05 (밤 3 — ②d 활성화 배포 + 백필가드 픽스)** — 로드맵 ② 라이브 가동.
  - **#111 머지** → main(c13125b). **배포**: `launchctl kickstart -k com.investchatbots.app`(launchd 관리)로 §1 재시작(phase=cycle_rest 유지). 새 PID·엔드포인트 라이브(/owner/seeds 307·/api/bottleneck/seeds 403).
  - **활성화**: `.env`에 `BOTTLENECK_CURATION_ENABLED=true` + `SITE_URL=https://investchatbots.com`(ntfy 절대링크). → **내일 05:50부터 6시 큐레이션 실가동**(하루 1콜 구독토큰).
  - **⚠️ 라이브 위험 발견·해소**: 백필은 `_bottleneck_seed()` 첫 호출 때만인데, 큐레이션이 그 전에 pending 넣으면 백필 영구 skip → **S 워치리스트 소실**. (1)라이브 DB에 14 approved 시드 **수동 백필로 즉시 보호** (2)픽스 `fix/curation-backfill-guard`: 큐레이션이 exclude 계산 전 `_bottleneck_seed()` 먼저 호출(향후 재발 방지). PR 대기.
  - **관전 팁**: 즉시 첫 후보 원하면 오너쿠키로 `POST /api/bottleneck/curate`(토큰 소모+폰 ntfy). 결재는 `/owner/seeds`.
- **2026-08-05 (밤 2 — 6시 큐레이션 에이전트 ②d + #108/#109/#110 머지)** — 로드맵 ② 완성.
  - **#108·#109 머지**(merge-commit, 스택 공유커밋 보존) → main. **#110 머지**(②a/②b/②c/②e) → main.
  - **②d(브랜치 `feat/bottleneck-curation`, 커밋 d935969)**: `run_bottleneck_curation()` — Claude 웹서치로 무명 상류 초크포인트 조사(하루 1콜) → pending 결재 큐. `_call_claude_cli`에 `allowed_tools` 추가(하위호환)로 헤드리스 웹서치. scheduler 05:50 mon-fri. 게이트 `BOTTLENECK_CURATION_ENABLED`(기본 off). 수동 `POST /api/bottleneck/curate`.
  - **설계 결정**: launchd 대신 **in-app 스케줄러**(서버 이미 Mac mini 로컬 DB 접근 → 단순). 비용은 **크레딧 아닌 구독 토큰 버킷**(agents가 ANTHROPIC_API_KEY 제거하고 CLI 구독토큰 사용) — 채팅·봇과 공유하는 그 5시간 버킷. 하루 1콜로 최소화.
  - **검증**: 헤드리스 웹서치 실동작 확인(`--allowedTools WebSearch` → Bloomberg 헤드라인). 실제 큐레이션 1콜 E2E(임시 DB) — **AXTI(InP 기판 공급난)·KLIC(HBM4 TCB 본딩)·MOD(액체냉각 백로그)** 발굴, 제외·pending·S결재·ntfy 정상. 신규 테스트 4건, 전체 189통과(사전 실패 11 무관).
  - **⚠️ 활성화 대기**: 이 브랜치 머지 → `.env` `BOTTLENECK_CURATION_ENABLED=true` → §1 재시작.
- **2026-08-05 (밤 — S 시드 큐레이션 인프라 ②a/②b/②c/②e)** — 브랜치 `feat/bottleneck-seed-db` → PR #110 머지. 커밋 3개(#98).
  - **②a/②b(커밋 059a2bb)**: 병목 시드 `strategy_private` 하드코딩 → DB `bottleneck_seed` 테이블(ticker·rationale·status·source). `source_bottleneck`이 approved만 소싱. 빈 테이블은 최초 1회 하드코딩 14시드 approved 백필 → **라이브 무접촉 보존**. `trading_engine`의 `US_BOTTLENECK_SEED` 직접참조(committee 유니버스)는 무변경.
  - **②c/②e(커밋 85bce59)**: `GET /owner/seeds`(폰 결재 페이지) + 오너 API 3종(목록/decide/add). `aifund.submit_bottleneck_candidates()` = pending 등록+S '결재 올림' 내레이션+오너 ntfy(②d가 부를 훅). ⚠️ `/owner/seeds`를 동적 `/owner/{token}`보다 먼저 선언(선점).
  - **검증**: 신규 테스트 6건 통과·전체 회귀 0(기존 실패 11건은 사전 존재=옛 로스터/채팅라우팅 드리프트, stash로 확인). TestClient E2E로 엔드포인트 구동(anon 403·오너 결재·400·페이지 렌더 200).
  - **⚠️ 남은 것 = ②d(6시 에이전트)** — 매일 API비용+launchd cron이라 가드레일 §3상 **명시 승인 후** 착수. submit 훅은 준비됨.
  - **⚠️ git**: 이 브랜치가 #108·#109(미머지) 위에 있음 → #108·#109 먼저 머지하면 이 PR diff가 깔끔. 아직 push 안 함.
- **2026-08-05 (대량 세션 — 라이브 버그 fix 9건 + S/Q 재설계 착수)**
  - **머지 완료(#101~107, origin/main)**: dotenv 로딩순서(NEW_DESK_ENABLED가 import시점 False로 굳던 컷오버 게이트 버그) · 관전피드 H봇 색누락 · 매수이유 모달 초보친화(A종목소개+friendlyReason, 원본 reasoning은 ranks 크레딧용 보존) · **yfinance 병목완화**(이름 영속캐시 db.stock_names + .info 3→2회 fail-fast + `/api/largecap/run` in-process 트리거) · **유저채팅 봇을 committee→P/W/H/S**(ROOT_IS_FUND 게이트, +desk='fund' 태그로 fund피드 노출) · **벤치마크 스냅샷 배선**(07-31 멈춤 — committee 스케줄러 은퇴 때 같이 빠짐 → scheduler에 잡 추가, SPY baseline 백필).
  - **미머지 PR**: #108(S 재소싱 버그) · #109(Q veto). 둘 다 CI 그린, 사용자 `!`로 머지 예정.
  - **인프라 원인 규명**: 34분 대형주 사이클이 **OOM으로 kill**(Mac mini 8GB, 스왑 87%). → 재개는 in-process(`/api/largecap/run`)로 메모리 안전하게. yfinance 병목이 사이클 길이 늘려 압박 가중.
  - **✅ S 재소싱 버그 수정(#108, 라이브)**: `source_bottleneck`이 영구 thesis캐시로 시드 제외 → 14 시드 소진 후 S 영구 휴면이던 것. '오늘분석+보유'만 제외하는 상시 워치리스트로. 소싱 0→4 복귀.
  - **✅ 섹터 정비(라이브, strategy_private)**: `우주·양자·로보틱스` 잡탕(RKLB우주+IONQ양자+ISRG의료로봇) 해체 → ISRG를 헬스케어로, `우주·항공`(RKLB·LUNR·RDW)·`양자컴퓨팅`(IONQ·RGTI·QBTS) 분리. **17→18섹터·63종목.** (나머지 16섹터는 응집 OK. V금융/MA핀테크 split은 미결.)
  - **✅ S 렌즈 보강(라이브, strategy_private)**: S 페르소나에 밸류렌즈 추가 — 병목픽은 저마진·적자여도 PER/ROE로 거르지 말고 시총vsTAM·희석으로. (S 페르소나는 원래 병목논지 중심이라 렌즈는 문제 아니었고, 재소싱이 진짜 원인이었음.)
  - **✅ Q veto 전환(#109, 라이브)**: 대형주가 P/W/H 승인해도 Q 진입신호(돌파/눌림) 하드게이트에 막혀 매수 0 + 청산은 Q단독 → 계좌 마름(비대칭). Q를 veto(약세장·200일선아래만 차단, 과매수는 오판위험이라 제외)로. 청산도 veto·추세매수는 M(50MA)로 덜 eager.
  - **진단 데이터**: 봇 매수율 P19%·H21%·**W3%·S2.5%**(OR게이트 사실상 P·H만 방아쇠). 관망은 데이터부족 아닌 실제 재무 깐깐거절. 대형주 전량 Q 청산돼 보유 0(→Q veto로 해결).
  - **🔬 공급망 병목(S 원형) 심층연구**: 기사+과거글 웹리서치. 핵심 — "참치(엔비디아) 말고 깻잎(무명 상류)", 논문·특허·capex로 초크포인트 발굴, **P/E 안 씀**(적자多), 시총vsTAM·선행PSR·인수가치·희석으로 판단, 초집중·고변동감내. **결론: 완전복제 불가**(봇이 그 리서치 못 함 + 반사성으로 픽 복제=펌핑끝물 매수) → "프레임만 취하고 발굴은 사람이 큐레이션". 상세 로드맵 상단 "🚧 다음 세션".
  - **⚠️ 배포 상태**: 라이브 서버는 `feat/q-veto-largecap` 워킹카피로 재시작됨(= main머지분 + #108재소싱 + #109Qveto + strategy_private 섹터/렌즈 전부 포함). #108·#109는 main 머지만 남음. main정렬은 머지 후 checkout+재시작.

- **2026-08-01 (저녁·관전 UX 재설계 결정 + fund 폴리시)** — 사용자와 대량 이터레이션. **결정 확정(빌드는 단계별 진행 예정)**.
  - **확정 결정**:
    - **닉네임 = 단일 알파벳 A·P·W·H·S·Q**(동물명 폐기). 코드·내레이션·UI 통일로 닉네임 불일치 버그 제거. ✅완료
    - **직위(職位) 시스템** = Members에서 역할 대신 직위. **올빼미(A)=신입 고정**, 나머지 한국 직급 사다리, **각 봇 '픽 성과'(그 봇이 매수 찬성한 종목들 평균 수익률)로 승진**(공유 데스크 유지·계좌 안 쪼갬). Q=아무 직위나. ⏳
    - **섹터 토론 → 종목 분석 2단계**(대형주): ①A가 "○○ 섹터 어때요?" → P/W/H 섹터 의견(형식 자유) → **리서치 보드에 섹터합의** ②종목별로 A가 TradingView 데이터 채팅 투척 → P/W/H 판단. **섹터=대형주 유니버스에 섹터 태그 도입**. ⏳
    - **종목 판단은 무조건 `[결정] {관망/매수/매도} | 이유: …` 포맷**(committee 포맷). 섹터 언급은 형식 자유. ⏳
    - **출근(초록) = 현재 단계 활성 봇만**. 06 대형주엔 S 꺼짐, Q는 P/W/H 핸드오프 후 켜짐. **초록 테두리 = 멤버 박스 전체(닉+직위+모델 감싸는 사각형)**. ⏳
    - **루트 컷오버**: `/` = fund, **committee 완전 제거**(라우트/토글 삭제, DB는 보존). ⚠️ fund 스케줄러 미배선이라 컷오버 후에도 자동매매는 Phase 2 배선 전까진 안 돎. ⏳
    - **깃 컨벤션 완화**: Epic이 수십개 안고가지 말고 **응집 묶음(1a~1d)=이슈 1개 + PR 몇개**. 단발=1이슈1PR. CONVENTION.md 반영 필요. ⏳
  - **이번 세션 완료(PR #84·#85 머지 + #86 브랜치 `fix/fund-price-emoji` 미머지)**: 이모지 제거 · **US 현재가 TradingView 우선+yfinance 폴백**(현재가 뜸) · Members 라벨 모델+역할(역할↑/모델↓) · **전략가 이름 전부 제거**(가치·성장주·텐배거·추세돌파·평균회귀→중립) · 태그라인 "인생 졸업을 향해" · 'A 리서치'→'리서치 보드' · 알파벳 닉네임 · **fund 청산 실현손익 표시**(payload에 pnl 필드 추가) · Q strat 감지 버그(개명 후 항상 B폴백) 수정 · 벤치마크 SPY·코스피·코스닥 · 현재섹터 box.
  - **Q 계좌 정리**: 레거시 `run_q_desk`(계좌 id6) 50개 완전 삭제. 백업 `bak-pre-qarchive-20260801-113755`.
  - **06 대형주 사이클 1회 테스트**(NEW_DESK_ENABLED 임시 패치): A소싱→P/W/H분석→관심종목 5(GOOGL·AMZN·NVDA·AVGO·TSM, **전부 H만 승인** — P/W는 yfinance 리밋으로 재무데이터 없어 관망)→Q집행(신규매수0, **아마존 청산 +17.57%**). **파이프라인 작동 확인. 단 yfinance 리밋이 재무·속도 반복 병목**(analyze_candidate는 아직 yfinance `info` 의존).
  - **S 확정**: 자체 소싱(US_BOTTLENECK_SEED) + OR게이트 자기픽 판단 유지(현 코드 그대로). 이유: 소싱이 S의 엣지, P/W 밸류렌즈에 안 죽게 OR게이트.
  - **✅ A단계(PR #87)**: `ranks.py` 직위(職位) 시스템 — 찬성봇 픽 평균 수익률로 한국 직급 사다리(신입→주임→대리→과장→차장→부장→이사→상무). A=신입 고정. 대형주 매수 reasoning에 '관심 P,W,H' 기록해 크레딧 보존. Members에 role→rank. **라이브 배포**(H=과장).
  - **✅ B단계(PR #87)**: 봇별 출근 판정(각 봇 최신 fund 내레이션 퇴근여부) → 06엔 S 자동off·Q 핸드오프후on. 초록 테두리를 **멤버 박스 전체**(닉+직위+모델)로. **라이브 배포**.
  - **✅ 피드 정리(D 일부 선처리)**: 옛 테스트 사이클 fund 메시지 46개 삭제(백업 `bak-pre-fundfeedclear-20260802-014312`). committee(desk NULL) 73809개 무관.
  - **✅ C단계(PR #91, C1~C3 통합)**: 대형주 섹터 토론 흐름 — **17섹터**(committee US 정합·59종목) · 2단계(섹터별 A질문→P/W/H 섹터의견→종목 `[결정] X|이유:` OR게이트, **매일 전 섹터 재분석**·캐시폐기) · 섹터합의 리서치보드 카드 · **재무 last-good 캐시**(리밋 폴백 → P/W/H 관망만 나오던 문제 해소). 미머지·라이브 재무검증은 API리밋으로 다음 실사이클 이월.
  - **✅ D단계(PR #92)**: 관전 피드 끊김 수정 — `trim_at_sentence`(350자 후 문장컷)가 응답 끝 `[결정]` 줄을 잘라 메시지가 결론 전 끊기고 관망 오폴백되던 버그. `call_agent(trim=)` 추가(기본 True, committee 무변경), fund 종목분석만 trim=False.
  - **✅ E단계(PR #94)**: 현재 섹터 박스 = 대형주 데스크 진행형(섹터명·N/17·종목칩, 오늘 섹터합의 리포트에서 도출). committee 스타일 복원.
  - **✅ F단계(PR #95)**: 루트 컷오버 게이트 `ROOT_IS_FUND`. 기본 off=committee 무변경(완전 되돌림), on=`/`=fund·토글숨김. **committee 라우트/스케줄러/DB 보존(라우팅만)**.
  - **✅ G단계(PR #93)**: 컨벤션 — Epic=응집 묶음, 테마 바뀌면 새 Epic. CONVENTION.md·CLAUDE.md §6.
  - **🎯 Epic #89 전 단계(A~G) 완료.** 머지 대기: E(#94)·F(#95). D·G는 머지됨.
  - **⚠️⚠️ 실제 컷오버(다음 큰 단계, 별도 승인)**: ①스케줄러 배선(06 대형주 A/P/W/H/Q · 12/18/24 발굴 A/P/W/S, 일봉 종가) ②`NEW_DESK_ENABLED=True` ③`ROOT_IS_FUND=true` + 재시작. **셋을 함께** 해야 `/`가 빈 피드 안 됨. 실매매·토큰 시작이라 §1·§4 가드레일. committee 은퇴는 이때.(A가 "○○섹터 어때요?"→P/W/H 섹터의견→리서치보드 섹터합의→종목별 A가 TradingView 투척→P/W/H 판단)+**종목판단 무조건 `[결정] {관망/매수/매도} | 이유:` 포맷**+대형주 유니버스 섹터태그 D)채팅 끊김 조사·수정 E)섹터 박스 데스크 진행형 배선 F)루트 컷오버(`/`=fund, committee 제거) G)CONVENTION.md(이슈=응집묶음). **⚠️ C에서 P/W 재무판단이 yfinance `info` 의존→리밋시 관망만 나옴, 재무 소스도 손볼 것.**
- **2026-08-01 (오후·통일 데스크 재설계)** — 4봇 경쟁/v1·v2 병존 구조를 **대형주/발굴주 2데스크로 통일, v1(committee) 대체**로 대전환. 설계 권위본 `docs/AIFUND_PIVOT.md` 갈아엎음.
  - **로스터**: A(발굴·리포트) · P/W/S/**H**(펀더, H=구 committee '실적왕' 개명·sonnet) · Q(대형주 타이밍 엔진). 4봇 경쟁·1억×4 폐기.
  - **구조**: 대형주(P/W/H OR게이트 → 관심종목 캐시 → **Q가 B+M 진입타이밍**·익절손절 + P/W/H 강한 펀더매도 청산) / 발굴주(A·S 발굴 → P/W/S OR게이트 즉시매수 → 논지청산). Q는 대형주 타이밍만(계좌 없음). "장기 메인 + 퀀트 보조지표" 철학.
  - **구현 1a~1e**(PR #76~80): 계좌·largecap_watch 캐시 · H페르소나·데스크로스터 · run_new_desk_cycle 4슬롯 재배선(run_largecap_select/execute·run_discovery_desk/review·source_largecap) · UI(2계좌·Members 모델만·나 유지) · Q 설명형 내레이션(haiku)+봇 핸드오프.
  - **머니**(백업 후): 대형주/발굴주 5000/5000 시드 + committee 장기에서 '실적왕+역추세봇 둘다 매수'한 **3종목(TSMC·엔비디아·아마존) 대형주 승계**(Option B: committee 원장 무접촉 복사). 백업 `bak-pre-deskmigrate-20260801-092858`.
  - **배포**: 라이브 8001 재시작 → `/fund` 통일 데스크 반영(대형주 3종목·발굴주). committee 무접촉(cycle_rest).
  - **일정 확정**(PR #81): 06 대형주(A/P/W/H/Q, US 종가 직후 신선 일봉) · 12/18/24 발굴(A/P/W/S). 전부 일봉 종가(장중 실행 폐기). 모든 사이클 매수까지 → 볼맛.
  - **v1 70/30 재분할**: 장기 7000만/트레이딩 3000만(비율 축소 재작성, 수익률 보존 4.69%/1.11%) + 벤치마크 bot_nav 재기준. 백업 `bak-pre-7030split-20260801-071503`.
  - ⚠️ **다음 = 컷오버**(Phase 2 스케줄러 배선 + NEW_DESK_ENABLED=True + committee 은퇴). 봇 실매매·토큰 시작이라 §1·§4 신중.
- **2026-08-01** — AI펀드 **v2 UI·소싱·내레이션 완성 + 방향 전환**(브랜치 `feat/fund-mode-chrome`, PR [#71](../../pull/71) 미머지).
  - **방향 전환**: 컷오버(committee 은퇴) 폐기 → **v1(committee)·v2(펀드) 둘 다 유지**, 좌상단 `v1↔v2` 토글(owner 전용). `/fund` = index.html 셸 재사용(`window.__FUND_MODE__`), v1 `/`는 무영향.
  - **v2 관전 UI**: Members(글자만·수익률 스파크라인·출근 초록테두리) · 관전 피드(봇색·봇별 출근/퇴근·A 분석완료·매번 다른 varied 멘트, `messages.desk='fund'`로 v1 피드 분리) · 2×2 사분면 가상계좌(티커·진입·현재·투자금) · **📋 A 리서치 패널**(섹터합의 자리, 사업요약 한글화[haiku]+핵심재무, 일반/병목 태그).
  - **소싱 철학 분리**: A=일반풀 발굴+전 후보 리포트 / P·W=A리스트 분석 / **S=`US_BOTTLENECK_SEED` 자체소싱**(발굴이 엣지) / Q=전 유니버스. `run_new_desk_cycle`을 `_process_pool`로 분리.
  - **DB 추가**: `messages.desk`(v1/v2 분리) · `fund_nav`(수익률 스파크라인) · `fund_report`(A 리포트). 오프라인 테스트 다수 추가, 회귀 0(기존 실패 10건은 옛-로스터 baseline 드리프트).
  - **보류(v1용, 사용자가 나중에)**: v1 실적왕→P+W+S OR게이트 재구현 · 장기/트레이딩 7000/3000 복원.
  - ⚠️ **다음**: PR #71 머지 → 라이브 배포(committee cycle_rest 후 §1 재시작) · Phase 2 스케줄러(봇별 시간 분산=라이브 배선 체크포인트).
- **2026-07-31** — AI펀드 피봇 대규모 설계·백테스트 세션. **Epic [#53](../../issues/53)** 로 묶음.
  - **AI펀드 정체 전환**: "협업 단일펀드"→**"전략별 계좌 겨루기"**. 로스터 P/W/S(장기·LLM)+R/M/B(트레이딩·룰봇). 설계 권위본 `docs/AIFUND_PIVOT.md` 갈아엎음. 드가자 삭제.
  - **committee v1 통째 백업**: git 태그 `committee-v1` + tarball(`~/Desktop/CSB/MyApps/InvestChatBots_committee-v1_2026-07-30.tar.gz`, strategy_private·DB·env 포함). 컷오버 전 복원점.
  - **코어(PR #48)**: P/W/S 발굴 분석(관대 게이트)·데이터 패킷 심화(마진·성장·FCF·사업요약)·T 하드 스탑. 페르소나는 실제 방법론 프레임워크로 심화(private).
  - **백테스트(PR #50)**: `backtest.py`에 regime(장세)별 분해 + M(추세돌파 트렌드템플릿+돌파) + M 시장필터(SPY 200MA) + 하드스탑 옵션 + **Q(B+M 블렌드) 정식화**(자본인지 `portfolio_sim`).
  - **핵심 발견**(US 514종목 6년): B(횡보/하락 강세)+M(상승 강세) **상호보완** → **Q 블렌드 +164%/MDD-11.7%**가 각 단독 압도. **단 Q≈QQQ(+166%)** — 생존편향 감안 시 아직 시장 못 이김, 낮은 낙폭이 잠정 강점. 하드스탑은 역효과(2%사이징이 이미 방어). R(로스)는 룰봇 최약→보류.
  - **Git 컨벤션**: Epic+Task 하이브리드 채택(#52) → 최종 정리(#62): **Task=PR만(이슈 안 만듦, 에픽번호·Part of #N), 단발만 Closes.** committee v1 백업(태그 `committee-v1`+tarball).
  - **오후: 구조 4봇 경쟁으로 확정 + Phase 1 실행배관 완성.**
    - **로스터 최종**: A(소싱·리서치브리프 룰) + **P·W·S·Q 4봇 각자 1억 독립 경쟁**(총 4억). P/W/S 위원회 폐지·각자 단독. Q=B+M 룰봇(전 유니버스 자체 스캔). R·D 삭제. **1북 아님 — 봇별 계좌.** 채팅=관전피드 재개념화.
    - **A 리서치 브리프(#56)**: 재무+**분기추세**(뉴스無·중립) → P/W/S 공용.
    - **Phase 1a(#60)**: 봇 계좌 4개(P/W/S/Q=id3~6, 각 1억, `ensure_fund_accounts` lazy seed).
    - **Phase 1b(#64)**: `execute_buys`(봇별 자기 계좌 매수)·`execute_thesis_sell`(논지 청산).
    - **Phase 1c(#66)**: `run_q_desk`(전 유니버스 B+M 스캔→Q 계좌), `backtest`의 M 전략 진입/청산 라이브 신호.
    - **하드스탑 제거(#65)**: 백테스트상 역효과+P/W/S는 논지로 청산 → 2% 사이징이 방어. 각 봇 자기 기준 청산.
  - ⚠️ **다음 세션 = Phase 2**(파이프라인+스케줄러). 전부 `NEW_DESK_ENABLED=False` 뒤 — 라이브(committee) 무영향.
- **2026-07-27** — 대규모 세션.
  - **Git 컨벤션·자동화 도입**: 이슈→`type/#N`브랜치→`[Type/#N]`커밋→PR→승인머지. 자동화(pr/issue-label·pr-title-lint·CI·delete_branch_on_merge). 커밋 쪼개기 가이드. allow리스트+`gh pr merge`는 ask. **머지는 항상 사용자 승인**·PR코멘트는 PR에서 응답.
  - **전략 비공개화**: 봇 페르소나 프롬프트·지표/튜닝 파라미터 → `strategy_private.py`(gitignore) + `*_example`(플레이스홀더 폴백). 앞으로 튜닝은 GitHub 미노출.
  - **자본 재편**: 트레이딩 2,500만→1억(플랫 500만/MAX20), 장기 7,500만→1억(티어 500/1000/1500만). 과거·현재 포지션 소급 재작성(백업).
  - **US-only 피벗**: KR 데이터 제거·재베이스, KR 사이클 off 토글(§4에 명시), US 섹터 9→17, 공급망 병목 병목 이론(발굴 시드 + 비투표 자문 봇).
- **2026-07-17** — (Agent Orchestration 롤아웃) PROGRESS.md 도입 + CLAUDE.md 상단에 세션 프로토콜 추가. 코드 변경 없음(docs).
