# InvestChatBots — CLAUDE.md (담당 클로드코드용)

> /clear 후에도 프로젝트 방향·규칙을 잃지 않게 하는 레포 steering 파일.
> 전략·OKR 원본은 오케스트레이터가 `okr/investchatbots.md`로 관리(권위본). 이 파일엔 "지금 뭘, 어떤 규칙으로" 만들지만 둔다.
>
> **구성**: 상단 = 스티어링(정체·왜·가드레일·지금 할 일·원칙). 하단 = 운영 절대 규칙(라이브 사이트 안전장치 — 어기지 말 것).
>
> ⚠️ **세션 프로토콜 (필수):** 시작 시 **[PROGRESS.md](PROGRESS.md) 먼저 읽어** 진행상황·다음 할 일 파악 → 작업 → **PROGRESS.md 갱신**. 이 CLAUDE.md는 *고정 지침·가드레일*, PROGRESS.md는 *살아있는 상태*다.
>
> ### 📌 정기 체크포인트 (2026-08-31 신설 — 세션 끝까지 미루지 말 것)
> `/clear`나 컨텍스트 압축은 **예고 없이** 온다. 세션 끝에 몰아 쓰면 그대로 날아간다.
> **아래 중 하나라도 해당하면 그 자리에서 PROGRESS.md를 갱신하고 커밋한다:**
> 1. **PR을 하나 열거나 머지했을 때** — 무엇을·왜 했는지 세션 로그에 한 줄
> 2. **사용자가 판단을 요구하는 제안이 나왔는데 답을 못 받았을 때** → `❓ 미답 제안`에 즉시 기록 (휘발 금지 규칙)
> 3. **구조·계좌·게이트·스케줄이 바뀌었을 때** → `🧭 시스템 전체 구성`을 **먼저** 고침
> 4. **코드 없이 얻은 발견**(운영 관측·트래픽 분석·버그 원인·사용자 행동) — 코드에 안 남으므로 **여기 안 적으면 영영 사라진다**
> 5. **사용자가 `/clear`·정리·마무리를 언급했을 때** — 비우기 전에 먼저 저장
>
> 갱신 대상 3곳: `updated:` 날짜 · 해당 섹션(1 시스템 구성 / 2 미답 제안) · 세션 로그.
> 문서 커밋도 컨벤션을 따른다 — `[Docs/#N]`, main 직접 커밋 금지.

---

# A. 스티어링

## 한 줄 정체
투자 스타일이 다른 AI 봇들이 매일 토론해 가상 포트폴리오로 모의 매매하고, 사람들이 그걸 관전하는 서비스. EverydayNews(아침 메일)가 유입구.

## 왜
ICB가 결국 내 계좌를 굴려 경제적 자유로. 그 험난한 여정 자체를 콘텐츠로 기록(매끈한 성공담 X). 이 사이클의 질문: "관전만으로 사람이 다시 오는가?"

## 🚧 콘텐츠 가드레일 (코드/콘텐츠 결정 시 절대 준수)
- **가상 포트폴리오**: 모든 매매·종목·수익에 "가상 포트폴리오 기준" 라벨. 실거래 아님.
- **권유 금지**: "사라/팔아라/오른다" 금지. 봇은 "내 가상계좌 관점은 ~"로만. 미래예측·수익보장 금지. 이미 일어난 사실만.
- **포지셔닝**: AI 종목추천 아님, "가상 토론 구경거리". 자문 아님.
- **토큰 = 한도 문제**: 구독 5시간 리셋 버킷을 Claude Code·채팅·봇이 공유. 채팅엔 서버단 하드 캡 필수.
- **방문통계 = 내부 백오피스**: 외부 공개·크롤링 우회·블로거 이름 공개 금지.

## 지금 할 일
**AI펀드 v2 빌드 (Epic #53) — v1(committee)·v2(펀드) 둘 다 유지.** `NEW_DESK_ENABLED=False` 뒤 additive — 라이브(v1) 무영향. **컷오버(committee 은퇴)는 폐기 → 좌상단 `v1↔v2` 토글로 둘 다 운영**(v1 실적 자산 보존 + v2 실험 병행 비교). v2 UI·소싱·내레이션 완성(관전 피드·2×2 사분면 계좌·📋 A 리서치 패널·S 자체소싱). 다음: **Phase 2 스케줄러**(봇별 시간 분산 + 라이브 오케스트레이터 배선 = 체크포인트) → forward 페이퍼. 설계 `docs/AIFUND_PIVOT.md`. 그 외 변경은 사용자와 확인 후.

### 채팅 = 관전 피드로 재개념화 (2026-07-31)
유저 질문 대화창 ❌ → **봇들이 일하는 걸 보는 read-only 관전 피드**(출근·매매·브리핑·퇴근). 유저 질문은 라이트하게 남길 수 있음(기본 서버캡으로 충분, 큰 큐 인프라 불필요). 남은 안전장치:
1. ~~서버 하드캡 + FIFO 큐~~ → **불필요**(관전 위주라 유저발 LLM 폭주 없음).
2. **면책 고지**: 가상 포폴 / 자문·권유 아님 / 책임 본인. — 유지.
3. **봇 권유금지 프롬프트 가드레일**: 콘텐츠 가드레일을 봇 응답 프롬프트에 강제. — 유지.

## 코딩 행동 원칙 (Karpathy 스타일)
1. **Think Before Coding** — 가정 혼자 세우지 마. 모호하거나 트레이드오프 있으면 짜기 전에 물어봐. (가드레일·결제·토큰 관련은 무조건) **API 호출 로직(AI 모델 호출·크롤링 등)을 사용자 승인 없이 자동 실행되도록 만들지 않는다.**
2. **Simplicity First** — 제일 단순하게. 200줄 될 거 50줄로. 불필요한 확장성·추상화 금지. 요청 안 한 기능은 자의로 추가하지 마. 시니어가 "과한데?" 하면 쳐내.
3. **Surgical Changes** — 요청받은 라인만. ICB는 라이브 사이트다. 멀쩡한 주변 코드·스타일·주석 건드리지 마. diff 작게.
4. **Goal-Driven Execution** — 검증 가능한 목표부터. 실패 테스트 먼저 → 통과 계획 → 루프로 검증. (예: 하드캡은 "(N+1)번째 거부" 테스트로 증명)

## 협업 규약
- 코드 실행·테스트·배포 = 클로드코드. 전략·우선순위·CFR = 오케스트레이터.
- 가드레일·핵심 결정 바뀌면 이 파일 + `okr/investchatbots.md` 둘 다 갱신.
- **미답 제안 휘발 금지**: 클로드가 제안했는데 사용자가 답 없이 다음 작업을 지시하면, 그 제안은 보류일 뿐 폐기가 아니다 — PROGRESS.md "미답 제안" 섹션에 기록하고 작업 마무리 보고 때 다시 묻는다.

---

# B. 운영 절대 규칙 (라이브 사이트 안전장치 — 어기지 말 것)

## 1. 서버 재시작 절차 (절대 규칙)

코드 수정 후 서버를 재시작할 때 반드시 이 순서를 따른다:

1. `pkill -f "uvicorn main:app"` — 서버 종료
2. **DB cycle_state 절대 건드리지 않고** 바로 서버 재시작
3. 재시작 후 `/api/state`로 phase 확인 — `cycle_rest`면 그대로 둔다
4. phase가 `sector_discussion`으로 잘못 돌아간 경우에만, **사용자에게 알리고 허락 받은 후** 수동 수정
5. 재시작 후 `/api/conversation/start`를 임의로 호출하지 않는다

---

## 2. cycle_state 리셋 금지 (절대 규칙)

- `cycle_rest` 상태 = 그 날 13개 섹터 분석 완료. 건드리지 않는다.
- `UPDATE cycle_state SET phase='sector_discussion'` 쿼리를 재시작 과정에서 실행하지 않는다.
- cycle_state 리셋은 오직 두 경우만 허용:
  1. 매일 6시 스케줄러가 `reset_daily_cycle()` 호출할 때
  2. 사용자가 명시적으로 "리셋해줘"라고 요청한 경우

---

## 3. API 비용 발생 작업 (사용자 명시 요청 시에만)

다음 작업은 **사용자가 직접 요청할 때만** 실행한다. 자동으로 돌리거나 코드 수정 후 테스트 명목으로 실행하지 않는다:

- 블로그 신호 추출 (`extract_signals_for_stock`) — Claude Haiku API 호출
- 텔레그램 메시지 강제 수집 (`fetch_telegram_messages(force=True)`) — 불필요한 호출 금지
- 요약 생성 (`/api/summary/request`)

---

## 4. 전체 동작 원칙 (절대 규칙 — 어기지 말 것)

모든 시간은 **한국 시간(KST) 기준**이다.

### 메인 사이클 (시장별 2회 — 2026-06-02 이원화)
- ⚠️ **2026-07-27: 국장(KR) 사이클 임시 off** (`orchestrator.KR_CYCLE_ENABLED=False`, US-only 집중 실험). 06시/09:05 KR은 no-op(벤치마크 스냅샷만 유지), 미장만 가동. **버그 아님 — 재개는 `True`로 바꾸고 서버 재시작.** 아래 KR 관련 설명은 재개 시 기준.
- **평일(월~금) 오전 6시 = 국장(KRX) 사이클** (11섹터), **오후 6시(18시) = 미장(US) 사이클** (9섹터). 스케줄러(`reset_kr_cycle`/`reset_us_cycle`)에 의해서만 시작된다.
- 현재 진행 시장은 `cycle_state.market`(전역 `_market`)이 단일 진실원천. `_active_sectors()`가 해당 시장 섹터만 반환한다.
- `run_round()`의 `if 0 <= hour < 6 and _market == "KRX": return` 가드는 **절대 제거 금지** — "6시 전 국장 실행 금지" 원칙. (진행 중 미장 사이클은 자정을 넘겨 마저 끝낼 수 있게 KR에만 적용.)
- 코드 수정/서버 재시작 중 `reset_market_cycle()` 호출, `conversation/start` API 트리거, DB cycle_state 수동 조작으로 임의 시작하는 것은 **절대 금지**.
- 재시작 후 phase가 `cycle_rest`면 그대로 둔다. 다음 사이클 시각(06시/18시)까지 기다린다.
- 사용자가 명시적으로 "지금 돌려"라고 말할 때만 수동 실행 허용.
- 주말(토·일)에는 두 사이클 모두 동작하지 않는다.
- 토큰이 부족하면 충전될 때까지 대기, 충전되면 자동 재가동.

### 보유 종목 점검 (각 메인 사이클 완료 직후 자동 — 해당 시장만)
- **각 메인 사이클이 `cycle_rest`로 진입하는 즉시** 백그라운드 스레드로 자동 실행 (`review_holdings(market=...)`).
- 국장 사이클 후엔 **KR 포지션만**, 미장 사이클 후엔 **US 포지션만** 점검 (`_advance_stock`에서 완료 시장을 캡처해 전달).
- 손절 체크(`evaluate_positions(market=...)`)도 동일하게 해당 시장 포지션만. 하루 2회(아침 KR / 저녁 US).
- 별도 cron job 없음. 메인이 안 도는 주말엔 자동 점검도 없음.
- 해당 시장의 열린 가상 포지션에 대해 7봇이 홀드 vs 매도 투표
- **매도 5표 이상**(과반) → 자동 매도 + 피드백 라운드 (기존 `_execute_sell` 경로)
- 그 외 → 보유 유지 (채팅 로그에만 기록)
- 9시 30분 손절 체크와는 별개. 매수 신규는 절대 발생하지 않음.
- 동시 실행 방지: `_holdings_review_lock` + `_phase != "cycle_rest"`면 자동 skip (`force=True`로 우회 가능)
- 수동 트리거: `POST /api/holdings/review` (force=True)

### 워치리스트 (서브 사이클 — 시장별)
- **메인 사이클이 완료(cycle_rest 진입)된 이후에만** 동작한다. 메인이 무조건 우선이다.
- 고정 시각: **12시 = 국장(KRX) 후보만** (`run_watchlist_kr`), **24시(00:00) = 미장(US) 후보만** (`run_watchlist_us`). 18시 워치리스트는 폐지(18시는 미장 메인이 차지).
- 대상: 오늘 메인 사이클 + 워치리스트에서 이미 다룬 종목은 제외. 하루에 한 종목은 한 번만 다룬다. 후보는 해당 시장(`market`)으로 필터.
- 봇 오류 시 재시도. 단, 토큰 소진 시는 즉시 중단 → 토큰 충전 후 다음 고정 시각에 자동 재개.
- 매수 조건: 과반수(5표 이상) 동의.

### 공통
- 이미 오늘 분석한 종목은 `daily_analysis_log`가 자동으로 건너뛴다. 재분석 명목으로 리셋하지 않는다.

---

## 5. 채팅 사용자 질문 처리

- 사용자가 채팅에서 종목을 물어보면 → 봇들이 답변 + ntfy 알림 발송 (자동)
- 채팅 질문은 실제 매수로 이어지지 않는다. 실제 매수는 오직 스케줄러 자동 분석에서 과반(국장 장기 데스크 7봇 중 4표 이상)일 때만.
- 자유로운 텍스트 질문에는 `[결정]` 포맷 없이 자연스럽게 대화한다.

---

## 6. GitHub 업데이트 (컨벤션 — 상세는 [CONVENTION.md](CONVENTION.md))

- **작업 단위 = Epic+Task 하이브리드** (상세 [CONVENTION.md](CONVENTION.md)):
  - **하나의 응집 묶음(1a~1d처럼 함께 가는 몇 Task) = Epic 이슈** `[Epic] #N - 주제` + **투두 체크리스트 본문**(언제든 편집). 그 아래 각 투두 = **Task PR**. **테마 바뀌면 옛 Epic에 얹지 말고 새 Epic**(예: 데스크 빌드 #53 ≠ 관전 UX #89).
  - **Task = PR만, 이슈 안 만듦**(이슈는 Epic 하나뿐). 제목 `[Type] #N - 내용`(**에픽 번호 N**), 브랜치 `type/슬러그`(`feat/q-exec`), 커밋 `[Type/#N]`, 본문 `Part of #N`(**Closes 안 씀**). 머지 시 에픽 투두 `- [x]` 체크(수동).
  - **단발(에픽 밖) = 1이슈=1PR**. 본문 `Closes #자기번호`(자동종료).
  - 한 Task/단발의 브랜치·커밋·PR은 **자기 번호 공유**, Type만 바뀔 수 있다. 한국어 설명 OK. (Type = Epic/Feat/Fix/Hotfix/Refactor/Design/Setting/Docs/Test/Chore)
- **main 직접 커밋·푸시 금지.** main은 안정 브랜치, 작업은 `type/#번호` 브랜치 → PR → diff 셀프 리뷰 → CI 통과 → 머지. (라이브 긴급 상황의 Hotfix만 예외, 사후 이슈화.)
- **의미 있는 단위마다** 커밋한다(기능1/버그1/리팩터1 = 커밋1). 세션 전체를 한 커밋으로 뭉치지 않는다. ❌ `update`·`wip`·`작업함` 같은 무의미 메시지·빈 커밋 금지.
- 세션 시작 시 `git fetch`로 origin 관계 먼저 확인 → 새 커밋 있을 때만 pull. 세션 끝엔 남은 변경 전부 커밋·push.
- 단, DB 파일(.db), 세션 파일(.session), .env는 절대 커밋하지 않는다(커밋 전 .gitignore 확인).

---

## 7. 봇 로스터 (운영 참조값)

> 코드 수정 행동 원칙은 상단 **A. 코딩 행동 원칙** 참조.

- **v1 = committee (라이브 유지)** (장기 7봇: 드가자, INTJ, 퀀트중독자, 빅픽처, 차트천재, 기본농부, 실적왕 / 트레이딩 5봇: 추세질주, 테마사냥꾼, 세력추적, 바닥픽, 칼손절). **은퇴 안 함** — 좌상단 토글의 v1. (아이디어: v1 실적왕을 P+W+S OR게이트로 재구현·장기/트레이딩 7000/3000 복원 — 보류, 사용자가 나중에.)
- **v2 = AI펀드 (설계: [docs/AIFUND_PIVOT.md](docs/AIFUND_PIVOT.md), Epic #53)** — `NEW_DESK_ENABLED=False` 뒤에서 개발 중, `/fund` 라우트 + `window.__FUND_MODE__`:
  - **A**(리서처 룰) = P/W용 일반 풀 발굴 + **모든 후보 리서치 리포트**(사업요약 한글화 + 핵심재무 → 📋 A 리서치 패널). 계좌 없음.
  - **P**(성장주)·**W**(가치) = A 리스트 분석 / **S**(병목) = **`US_BOTTLENECK_SEED` 자체 소싱**(A 리스트 안 받음, 발굴이 엣지) / **Q**(B+M 룰봇) = 전 유니버스 자체 스캔.
  - **4봇 경쟁**(P·W·S·Q **각자 가상 1억 독립 계좌**, 각자 매수·매도, 총 4억). A는 소싱·리포트만. D 삭제 · R 보류.
  - 관전 UI: Members(수익률 스파크라인·출근 초록테두리) · 관전 피드(봇색·봇별 출근/퇴근·A 분석완료·varied 멘트, `messages.desk='fund'`로 v1 분리) · 2×2 사분면 계좌(티커/진입/현재/투자금).
  - **컷오버 없음** — v1과 병행. `NEW_DESK_ENABLED=True` + 스케줄러 배선이 라이브 활성 단계.
